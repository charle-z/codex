name: blocking-ci-validation-disabled

on:
  workflow_dispatch:

jobs:
  validation-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Broad blocking CI is intentionally disabled on this validation-only base branch."
