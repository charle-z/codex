name: blocking-ci

# Validation branch only: keep the upstream fan-out disabled.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "blocking-ci disabled on isolated validation base"
