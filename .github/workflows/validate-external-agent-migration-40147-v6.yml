name: validate-external-agent-migration-40147-v6

on:
  pull_request:
    branches:
      - validation/base-40147-e7777308
    paths:
      - "codex-rs/external-agent-migration/**"

permissions:
  contents: read

jobs:
  migration:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: fix/40147-preserve-external-agent-references-v6
          fetch-depth: 0
          persist-credentials: false
      - uses: ./.github/actions/setup-ci
      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          components: rustfmt, clippy
      - uses: taiki-e/install-action@44c6d64aa62cd779e873306675c7a58e86d6d532 # v2.62.49
        with:
          tool: nextest
          version: 0.9.103
      - name: Assert exact source shape
        run: |
          test "$(git rev-parse HEAD)" = "1103afbc1a8fc89e60538ff95796610f0e11aa00"
          test "$(git rev-list --count e77773085cf770c89b9f0800220786c15f38d113..HEAD)" -eq 1
          test -z "$(git rev-list HEAD..e77773085cf770c89b9f0800220786c15f38d113)"
          files="$(git diff --name-only e77773085cf770c89b9f0800220786c15f38d113...HEAD | sort)"
          expected=$'codex-rs/external-agent-migration/src/rewrite.rs\ncodex-rs/external-agent-migration/src/rewrite_tests.rs'
          test "$files" = "$expected"
      - name: Test external-agent migration crate
        run: just test -p codex-external-agent-migration
      - name: Apply scoped clippy fixes
        run: just fix -p codex-external-agent-migration
      - name: Run scoped clippy
        run: just clippy -p codex-external-agent-migration -- -D warnings
      - name: Check Rust formatting
        working-directory: codex-rs
        run: cargo fmt -- --config imports_granularity=Item --check
      - name: Verify validation did not change tracked files
        run: git diff --exit-code
