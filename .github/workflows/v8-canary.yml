name: v8-canary

# Validation branch only: keep the expensive canary disabled.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v8-canary disabled on isolated validation base"
