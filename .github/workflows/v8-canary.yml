name: v8-canary-validation-disabled

on:
  workflow_dispatch:

jobs:
  validation-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "V8 canary is intentionally disabled on this validation-only base branch."
